# Pre-built tokenizer static libs — downloaded via scripts/download-libtokenizers.sh
# Not committed; too large (37 MB per platform) and platform-specific.
lib/*/libtokenizers.a

# Built binaries (match only files at repo root, not source directories)
/susfactor-check
/my-service
