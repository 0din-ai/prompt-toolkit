# Showcase benchmark dependencies
# Install with: pip install -r demos/requirements.txt
#
# Also requires the odin-prompt-toolkit Python package itself:
#   pip install -e "packages/python[onnx]"

# Vector search comparisons
sqlite-vec>=0.1.6          # SQLite vector extension (brute-force KNN)
pgvector>=0.4.0            # pgvector Python bindings
psycopg[binary]>=3.1.0     # PostgreSQL driver (for pgvector)

# Output formatting
tabulate>=0.9.0            # Pretty tables

# Progress bars
tqdm>=4.66.0               # Progress bars for embedding generation

# Numerical
numpy>=1.24.0              # Already required by signature_sdk, listed for clarity
